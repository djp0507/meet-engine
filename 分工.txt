Android

UI => 谢天宇
1. 字幕样式渲染（字体，大小，颜色，边框，透明度，布局）
2. 用户自定义的字幕样式（字体，大小，颜色，边框，透明度，布局）
3. 字幕多语言选择
4. 字幕字符集设置
5. 多音轨选择

播放逻辑层 => 谢天宇
1. 字幕文件选择的逻辑控制
2. 解析备选的字幕语言种类
3. 集成字幕解析库
4. 同步字幕，通知UI层做显示

基础库 => 沈学成
1. 全格式的音视频同步，解码，渲染

IOS

UI => 钱国强，张煌
1. 集成自有播放器。统一播放控制UI, 能够灵活切换系统播放器与自有播放器
2. 字幕样式渲染（字体，大小，颜色，边框，透明度，布局）
3. 用户自定义的字幕样式（字体，大小，颜色，边框，透明度，布局）
4. 字幕多语言选择
5. 字幕字符集设置
6. 多音轨选择

播放逻辑层 => 汪坤，张煌
1. 集成基础库，提供与mediaplayer一致的播放接口供UI层集成。
2. 本地媒体库管理
3. 字幕文件选择的逻辑控制
4. 解析备选的字幕语言种类
5. 集成字幕解析库
6. 同步字幕，通知UI层做显示

基础库
1. 字幕解析库，支持srt, ass, smi, vobsub => 汪坤
2. 全格式的音视频同步，解码，渲染 => 沈学成